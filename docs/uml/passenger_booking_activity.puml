@startuml
start
:普通用户登录;
:查看可预约车次;
if (车次可预约且有余座?) then (是)
  :选择座位;
  if (是否重复预约?) then (是)
    :提示不能重复预约;
    stop
  else (否)
    :生成预约订单;
    :订单状态=待发车;
    :支付状态=待支付;
    :推送预约已创建通知;
  endif
else (否)
  :提示车次不可预约或已满座;
  stop
endif

if (用户立即支付?) then (是)
  :选择微信支付或支付宝;
  :创建支付流水;
  :支付状态=支付中;
  :Mock 支付完成;
  :支付状态=已支付;
  :推送支付成功通知;
else (否)
  :保留待支付订单;
endif

if (用户取消预约?) then (是)
  if (预约待发车且未发车?) then (是)
    :计算取消扣分;
    :预约状态=已取消;
    :扣减信用分;
    :推送信用通知;
  else (否)
    :提示不可取消;
  endif
endif
stop
@enduml
